doctype
html(lang="en")
head
    meta(charset="UTF-8")
    title Oauth2
body
    p Hi #{user.username}
    p 
        b #{client.applicationName} is requesting access to your account
    form(action="/auth/decision", method="POST")
        input(type="hidden", name="transaction_id", value="#{transactionId}")
        input(type="submit", value="Allow")
        input(type="submit", value="Deny", name="cancel")
    
